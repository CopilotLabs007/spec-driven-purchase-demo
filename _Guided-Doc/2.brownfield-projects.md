1. Inspect existing Express routes and seed data.
2. Generate a baseline API specification from the current implementation.
3. Validate the generated spec with curl/integration tests.
4. Identify drift or undocumented behavior.
5. Update the spec with the desired change.
6. Update the existing implementation to match the revised spec.
7. Add regression tests so existing behavior stays stable.

Greenfield: spec -> code
Brownfield: code -> baseline spec -> validated spec -> changed spec -> code